# Local configuration for the stdio-only MCP process. Copy this file to the
# ignored services/mcp/.env, set MCP_SERVICE_PASSWORD, then restrict it with
# chmod 600 services/mcp/.env.
INGESTOR_URL=http://localhost:8000
MCP_SERVICE_USERNAME=mcp-service
MCP_SERVICE_PASSWORD=replace-with-a-strong-local-password
