meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "{{username}}",
    "email": "admin@example.com",
    "password": "{{password}}"
  }
}

assert {
  res.status: in [201, 409]
}
