meta {
  name: Create Source probe-fail
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/sources
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "probe-fail",
    "base_url": "https://httpbin.org",
    "health_check_path": "/status/500",
    "probe_interval_seconds": 10,
    "latency_threshold_ms": 100,
    "is_active": true
  }
}

assert {
  res.status: in [201, 409]
}
