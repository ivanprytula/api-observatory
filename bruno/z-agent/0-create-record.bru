meta {
  name: Setup — Create Test observation
  type: http
  seq: 0
}

post {
  url: {{baseUrl}}/api/v1/observations
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "source": "test.agent.seed",
    "data": {"test": true, "value": 42}
  }
}

assert {
  res.status: in [200, 201]
  res.body.id: isDefined
}

script:post-response {
  if (res.status === 201) {
    bru.setEnvVar("observation_id", res.body.id.toString());
  }
}
