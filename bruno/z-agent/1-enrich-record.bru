meta {
  name: Enrich observation (full run)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/agent/enrich/{{observation_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
