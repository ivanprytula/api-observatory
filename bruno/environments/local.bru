vars {
  baseUrl: http://localhost:8000
  username: admin
  password: admin123
  token:
  refresh_token:
  source_id: 1
}

# For HTTPS via nginx, use: baseUrl: https://localhost (run `just up-https` + certs first)
vars:secret [token, refresh_token]
