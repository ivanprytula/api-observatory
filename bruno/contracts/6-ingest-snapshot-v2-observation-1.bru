meta {
  name: Ingest snapshot v2 — observation 1/3
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/contracts/snapshots
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "source_id": {{drift_source_id}},
    "payload_schema": {
      "userId": "integer",
      "id": "integer",
      "body": "string"
    },
    "schema_version": "2.0.0",
    "snapshot_note": "After migration — title field removed"
  }
}

assert {
  res.status: eq 201
  res.body.drift_event: eq null
}
