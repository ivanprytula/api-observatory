---
# Install Galaxy collections before running any playbook:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: 'ansible.posix'
    version: '>=1.5.0'
  - name: 'community.docker'
    version: '>=3.0.0'
  - name: 'community.general'
    version: '>=9.0.0'
  - name: 'amazon.aws'
    version: '>=6.0.0'
  - name: 'google.cloud'
    version: '>=1.3.0'
