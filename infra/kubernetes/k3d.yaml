apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: api-obs
servers: 1
agents: 0
ports:
  - port: 8080:80
    nodeFilters:
      - loadbalancer
options:
  k3d:
    wait: true
    timeout: 120s
registries:
  create:
    name: api-obs-registry
    host: "127.0.0.1"
    hostPort: "5000"
