apiVersion: v1
kind: Service
metadata:
  name: ingestor
  labels:
    app.kubernetes.io/name: ingestor
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: api-obs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ingestor
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
